// Fixture stub for VDD golden tests. Not real code.
// Emits broadcast signals: auth:verified / auth:expired.
export class AuthSession {
  verify(): void {
    // emit "auth:verified"
  }
  expire(): void {
    // emit "auth:expired"
  }
}
